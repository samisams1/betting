import React from "react";
import FixturesLayout from "./layout";

const Fixtures = ({ title, matches, pastMatches }) => (
  <FixturesLayout title={title} matches={matches} pastMatches={pastMatches} />
);

export default Fixtures;
